---
topic: Agentic Wiki continuous Codex development handoff
goal: Enable unattended-safe Codex development through parallel TDD, fallback work queues, and explicit blocking policy
updated: 2026-07-08T04:51
---

- (direction) User confirmed BMAD initialization in this Agentic Wiki worktree using project name Agentic Wiki, stage brownfield, starting path spec, default artifacts, and intermediate skill level.
- (decision) Use bmad-spec path to create a continuous Codex development handoff contract for Agentic Wiki rather than a generic delivery playbook.
- (decision) Spec slug is agentic-wiki-continuous-codex-development and output folder is _bmad-output/specs/spec-agentic-wiki-continuous-codex-development.
- (capability) CAP-1: Future Codex threads can load one spec bundle and identify the exact repo rules, source surfaces, validation gates, and first safe task without relying on chat history.
- (capability) CAP-2: Codex can decompose approved work into independent TDD slices and run safe parallel agents with declared ownership boundaries.
- (capability) CAP-3: Codex can continue useful low-risk work when one task blocks by moving to a fallback queue without fabricating missing decisions or crossing PHI and repo boundaries.
- (capability) CAP-4: Codex can preserve Agentic Wiki public contracts across canonical agentic_wiki and legacy memwiki API and CLI surfaces.
- (capability) CAP-5: Codex can keep generated documentation, capability manifests, schema/workflow documentation, and tests aligned with behavior changes.
- (capability) CAP-6: Codex can enforce clinical PHI local-only constraints with OperationContext requirements and synthetic-only development data.
- (constraint) Use tests before behavior changes; required closeout commands are uv run pytest, uv run ruff check ., uv run mypy src, and docs checks when affected.
- (constraint) Canonical memory pages are semantic HTML with JSON-LD metadata; generated knowledge must enter drafts before promotion and raw sources remain immutable after ingest.
- (constraint) AgenticWikiWorkspace from agentic_wiki is the canonical public integration surface, while memwiki.MemwikiWorkspace and memwiki CLI remain compatibility shims unless removal is explicitly approved.
- (constraint) Clinical PHI workspaces require OperationContext for PHI-reading or mutating operations, use no actual PHI in development artifacts, and remain strict local-only.
- (constraint) Machine-readable JSON outputs for agent-facing commands and .memwiki/agent-capabilities.json mutation policy alignment are contract surfaces.
- (constraint) Implementation agents must preserve dirty-tree boundaries, avoid overwriting user-authored artifacts, and keep CLI commands as thin wrappers around public API.
- (decision) Use spec-authored companions for multi-agent topology, task graph, TDD contract, blocking policy, validation contract, and handoff runbook.
- (decision) Treat AGENTS.md and README.md as adopted context that future agents should read alongside the spec, while docs and API files are absorbed into the project-specific companions.
- (assumption) The target software for this spec is the current Agentic Wiki repo in /Users/tyler-lcsw/.codex/worktrees/6999/memory_system.
- (assumption) Default BMAD artifact paths are acceptable because the user confirmed the proposed intake.
- (question) Should future continuous-development threads be allowed to create branches and commits automatically, or should they stop before git commit/push unless separately instructed?
- (question) What maximum parallel-agent count should the runbook use by default for this repo when the user does not specify one?
- (event) Coherence validation pass: capability IDs are unique, each capability has intent and success in the rendered spec, constraints bend implementation, non-goals are explicit, and prose is kept lean.
- (event) Preservation validation pass: confirmed intake, README, AGENTS instructions, architecture and operations docs, HIPAA-local docs, API surface, and prior repo memory are represented in SPEC.md or companions.
- (event) Wrapper-only content dropped: BMAD activation chatter and thread metadata were not copied because they do not change downstream build or validation decisions.
- (event) Spec artifacts rendered from memlog and source context.
- (event) Repository validation passed after spec render: uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User set default Codex autonomy to continuous delivery with full unsandboxed access: future sessions may branch, edit, test, stage, commit, push, open PRs, and continue fallback work unless the active request narrows scope.
- (constraint) The only Codex-specific autonomy boundary is that computer-use, GUI-driving, or human-desktop-control workflows require user supervision.
- (event) Validation passed after autonomy update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User resolved default parallelism: future Codex sessions may use one coordinator plus up to six simultaneous workers for this project unless the active request narrows scope.
- (event) Validation passed after six-worker parallelism update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User agreed that BMAD artifacts are the authoritative work source; agent-derived safe fallback tasks are allowed only when BMAD work is empty, blocked, or silent on the next safe action.
- (event) Validation passed after authoritative work-source update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User set continuous-run stop order: first obey explicit time, token, work, budget, or platform usage limits; otherwise continue until BMAD and safe fallback queues are drained and associated tests pass.
- (constraint) Known host or Codex usage limits are hard stops; close cleanly with state, remaining queue, validation status, and resume instructions before losing context when possible.
- (event) Validation passed after continuous-run stop policy update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User confirmed continuous runs should write a durable resume artifact under _bmad-output/implementation-artifacts/ when stopping before queue drain.
- (decision) User clarified PR workflow: traditional PR approval before merge and continued development runs contrary to the objective, so PRs are non-blocking audit/synchronization artifacts rather than approval gates.
- (event) Validation passed after resume artifact and PR workflow update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User agreed to branch naming defaults: codex/<date>-<short-goal> for user-facing runs and codex/auto/<date>-<queue-slice> for unattended queue slices.
- (decision) User required a living HTML branch ledger with table columns branch name, date/time last updated, and brief description of contents; ledger path is _bmad-output/implementation-artifacts/branch-ledger.html.
- (event) Validation passed after branch naming and branch-ledger update: rg artifact check, HTML table header parser check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User accepted branch ledger cadence recommendation: update the living HTML ledger after every material branch change, not only major milestones.
- (event) Validation passed after branch ledger cadence update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User confirmed validation and blocker handling: keep parallel slices identified at all times; a blocker or validation failure in slice A stops only that slice and dependent slices, while development moves to independent slice B and reports blocked behaviors when the user returns.
- (event) Validation passed after parallel-slice validation/blocker handling update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved per-slice run-state tables: every resume artifact must include slice statuses active, blocked, waiting-on-dependent, validated, merged, and superseded.
- (event) Validation passed after per-slice run-state table update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved slice-creation rule: workers may propose independent slices, but the autonomous coordinator creates and assigns slices; user approval is not required for safe new slices inside existing BMAD/spec boundaries.
- (event) Validation passed after autonomous coordinator slice-creation update: rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User accepted machine-readable slice queue recommendation: maintain _bmad-output/implementation-artifacts/slice-queue.json so future agents can resume and dispatch workers without parsing prose.
- (event) Validation passed after machine-readable slice-queue update: JSON schema smoke check, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User confirmed slice-queue.json is the source of truth for active slice state and requested well-formed HTML instead of Markdown for applicable human-facing continuous-run artifacts; BMAD-required SPEC.md artifacts remain Markdown.
- (event) Validation passed after slice-queue source-of-truth and HTML artifact format update: rg artifact check, JSON source-of-truth check, HTML template parser check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (constraint) Before the first real continuous run, implement and validate a renderer script that derives run-state HTML from slice-queue.json; production continuous runs must not rely on manually edited run-state HTML.
- (event) Validation passed after run-state renderer preflight update: uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User evolved the goal: execute the Agentic Wiki integration plan without Cloudflare and keep the foundation provider-agnostic for future agent development projects; plugin creation is future work, not part of this implementation slice.
- (event) Implemented provider-agnostic agent-development memory foundation with run-state renderer, draft workflow, API/CLI commands, capability metadata, docs, tests, branch ledger update, and validated slice queue entry.
- (event) Generated first real run-state HTML artifact from slice-queue.json using the new agent render-run-state command at _bmad-output/implementation-artifacts/run-state/2026-07-07-agent-development-memory.html.
- (event) Final validation passed after agent-development memory integration: artifact HTML/JSON-LD validation, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved browser-tool autonomy boundary: scoped browser or Chrome-control plugins may run autonomously for development verification, while broad human-desktop control and external sensitive actions require supervision.
- (decision) User required a living HTML supervision queue: user return appends or prioritizes supervision-required tasks, but does not necessarily interrupt active safe work; queued items become priority at the next reasonable stopping point.
- (constraint) Agents must exhaust authorized problem-solving resources before stopping for ordinary difficulty, including repo/docs/tests, Agentic Wiki memory, web search, official docs, installed skills/plugins, scoped browser tooling, parallel review, and independent fallback slices.
- (event) Validation passed after problem-solving boundary and supervision-queue update: HTML/JSON-LD artifact validation, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved the mandatory problem-solving escalation ladder order: local repo/tests/logs/docs/BMAD/Agentic Wiki memory; package/tool output and official project docs; web search/current official ecosystem docs; installed skills/plugins and scoped browser verification; parallel/adversarial review; then slice splitting, independent-slice continuation, or supervision-queue entry.
- (constraint) The problem-solving escalation ladder must be followed in order unless a time, budget, safety, permission, privacy, or task-applicability boundary prevents a step; attempts and skipped steps must be recorded in blocker notes or run-state evidence.
- (event) Validation passed after mandatory escalation ladder update: HTML/JSON-LD artifact validation, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved BMAD-aware but not BMAD-dependent plugin behavior: projects with _bmad use BMAD artifacts, while non-BMAD projects use lightweight project-local agent-development memory with equivalent primitives.
- (constraint) Agent-development projects must not mutate the reusable plugin, plugin cache, marketplace entries, global skills, or plugin defaults to encode project-specific shape, formation, guidance, decisions, queues, or run state; those outputs live in project-local memory unless the user explicitly starts plugin development.
- (event) Validation passed after BMAD-aware plugin boundary and project-memory separation update: HTML/JSON-LD artifact validation, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved project-local plugin overlays: each project may customize plugin output, guidance, and behavior through project memory applied after plugin defaults, while the core plugin remains immutable unless the user explicitly starts plugin development.
- (decision) User approved a project-local plugin improvement queue so reusable improvement candidates are captured as proposals without mutating the plugin during project work.
- (event) Validation passed after project-local plugin overlay and plugin-improvement queue update: HTML/JSON-LD artifact validation, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved instruction and overlay precedence order: system/developer/user safety and active request; repo-local AGENTS.md and explicit user project instructions; BMAD artifacts when present; project-local plugin overlay memory; plugin defaults; agent judgment.
- (event) Validation passed after instruction-precedence update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved project-local overlays broadening agent autonomy within higher-priority boundaries because each project may require different agent skill sets.
- (event) Validation passed after overlay-autonomy broadening update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User directed that project setup should use intent-to-resource scoping language: the plugin translates user intent into needed resources and skill sets, not sandbox or autonomy-profile framing.
- (event) Validation passed after intent-to-resource scoping language update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User resolved missing-resource handling: if an expected resource should be available but is not, stop that slice and notify the user in chat; if the resource does not exist but would be useful, continue with available resources and note the future resource opportunity to the user.
- (event) Validation passed after resource-gap handling update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User required future-resource opportunities to have their own living semantic HTML queue, separate from blockers and plugin-improvement candidates.
- (event) Validation passed after future-resource queue update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User required future-resource queue entries to remain advisory until explicitly promoted by the user or an explicit plugin-development workflow.
- (event) Validation passed after advisory future-resource promotion update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User directed future-resource queue entries to remain a simple chronological advisory queue, with no priority/status triage; move on from the advisory feature after recording this.
- (event) Validation passed after simple chronological future-resource queue update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved agents making reversible implementation-level decisions autonomously mid-run, provided the decisions and rationale are recorded in run evidence.
- (event) Validation passed after reversible implementation decision-authority update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved distinguishing implementation, architecture, and product decisions in run evidence so future agents can separate reversible local choices from higher-authority choices.
- (event) Validation passed after decision-evidence taxonomy update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User accepted recommendation that unresolved architecture or product decisions stop only the affected slice and dependents while independent slices continue; pause the whole run only when the decision affects the run boundary, shared architecture, safety constraints, or all remaining work.
- (event) Validation passed after higher-authority decision blocking update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved validation failure handling recommendation: allow two bounded self-repair attempts inside the failing implementation slice, then stop that slice, record failure evidence, preserve or revert only slice-owned changes as appropriate, and continue independent slices without rolling back unrelated user changes or other slices.
- (event) Validation passed after validation failure repair-loop update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved parallel worker coordination recommendation: only the coordinator may mutate shared control artifacts such as slice-queue.json, branch ledger, run-state artifacts, supervision/resource/plugin queues, and project overlays; workers modify only assigned code, docs, or tests and submit worker reports with proposed control-artifact changes for coordinator acceptance after validation.
- (event) Validation passed after shared control artifact ownership update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved unattended status reporting recommendation with one addition: routine progress should update local artifacts instead of chat; chat notifications are reserved for expected-but-unavailable resources, exhausted repair loops, supervision-required tasks, whole-run stops, safety/privacy boundaries, and user-return queue presentation; a chat summary must be available on request.
- (event) Validation passed after status reporting and chat notification policy update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User rejected a hardline credential boundary as unrealistic and set a pragmatic development credential rule: if the user chooses to pass a credential in chat, agents may use it for development work while making best efforts not to store, echo, log, or commit it; assume development credentials, policies, and passwords differ from production.
- (event) Validation passed after development credential handling update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved mandatory non-local source evidence: when web search, official docs, browser verification, installed plugins, or other non-local sources justify implementation, blockers, or architecture/product decisions, run evidence must record source URL or tool name, retrieval/check time, supported claim, and whether it was official or current enough without copying large source content.
- (event) Validation passed after non-local source evidence update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved high-risk review gate: slices touching PHI/privacy, credentials, public API compatibility, schema/storage layout, plugin-core behavior, generated docs contracts, security boundaries, or deployment/publishing behavior require review-worker or adversarial review before merge or integration; ordinary low-risk implementation slices may rely on tests plus coordinator review.
- (event) Validation passed after high-risk review gate update: HTML/JSON-LD artifact validation, rg artifact check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved dirty-worktree intake: every continuous run starts by classifying existing worktree changes by path as active slice, prior-agent work, user/unowned work, or generated artifact before dispatching workers; preserve unowned changes and stop overlapping slices with unclear dirty files while continuing independent slices.
- (event) Validation passed after dirty-worktree intake update: artifact rg check, HTML/JSON-LD artifact validation, slice-queue JSON check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved mandatory preflight readiness gate before continuous-run worker dispatch: coordinator verifies spec and handoff loaded, slice queue parseable, dirty worktree classified, branch ledger current, run-state renderer available, validation baseline selected, resource/credential/supervision needs identified, and at least one safe slice ready; failures become enabling or blocker slices and the whole run stops only when no independent work exists or a stop boundary applies.
- (decision) Plugin primary input is a project-local intent packet normalized from either a guided preflight spec interview, existing specs/resources referred by the user, or a hybrid resource referral plus adaptive clarifying questions; the plugin asks only the additional questions needed to form a clear actionable work picture.
- (decision) Plugin should suggest natural user check-in points at major milestones, but when the user indicates long unattended continuous development, check-ins remain advisory unless a supervision or stop boundary is reached and the coordinator prioritizes queue progress with durable local status artifacts.
- (event) Validation passed after preflight readiness, intent-packet input model, and milestone check-in update: artifact rg check, HTML/JSON-LD artifact validation, slice-queue JSON check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved dual-form intent packets: each project-local intent packet should have machine-readable JSON as dispatch input and human-facing semantic HTML as the user/review surface.
- (decision) User directed the plugin to do substantial early project-scope classification: interview paths must adapt by project archetype because a WordPress website and a web-based application require different questions, resources, validation, and delivery slices.
- (decision) User directed that projects with visual or frontend concerns need graphic-design agreement during intake; Codex should use image generation and installed design/UX skills to suggest visual pathways during the interview and in creating the intent-packet HTML review form, with the user selecting or approving the direction.
- (event) Validation passed after dual-form intent packet, domain-specific intake, and design-assisted intake update: artifact rg check, HTML/JSON-LD artifact validation, JSON artifact validation, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved a small built-in archetype registry for the future plugin: WordPress/content website, web application/SaaS, CLI/library, mobile/desktop app, data/automation workflow, clinical/regulated workspace, and infrastructure/devops tooling; each archetype defines discriminating questions, likely design needs, validation baseline, high-risk surfaces, and common slice patterns, with project-local memory allowed to add or override guidance without mutating the plugin.
- (event) Validation passed after archetype registry integration: uv run pytest; uv run ruff check .; uv run mypy src; uv run agentic-wiki docs check.
- (decision) User approved mixed-project archetype handling: select one primary archetype for orchestration and attach secondary archetypes as scoped subprofiles or slices; conflicts stop only affected slices unless they change the whole project boundary.
- (event) Validation passed after mixed-archetype policy integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved three intake depths for the future plugin: quick start, standard preflight, and deep spec; unattended continuous development requires at least standard preflight for the primary profile, while secondary profiles need essential fields unless they become high-risk.
- (event) Validation passed after intake-depth policy integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved project-local storage adapter model for the future plugin: use a BMAD adapter when _bmad exists and a lightweight Agentic Wiki adapter otherwise; both expose the same primitives for intent packet, run-state HTML, branch ledger, supervision queue, resource queue, decision log, and project plugin overlay.
- (event) Validation passed after storage-adapter model integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved multi-skill plugin shape for the future plugin: one plugin with focused skills for intake/preflight, continuous-run coordination, status/report handoff, and plugin-improvement review; shared schemas, templates, and scripts live in the plugin while project-specific output goes through the selected storage adapter.
- (event) Validation passed after multi-skill plugin shape integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved skill-level mutation permissions for the future plugin: intake/preflight writes only intent, readiness, queue, and resource artifacts; continuous-run coordinator may edit code, docs, tests, and shared control artifacts; status/report handoff is read-only except generated reports; plugin-improvement review may write candidate analysis but cannot modify plugin files unless the user explicitly starts plugin-development work.
- (event) Validation passed after skill-level mutation-permission integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved explicit plugin run modes in the intent packet: interactive-intake, unattended-continuous-run, status-only-report, and plugin-improvement-review. The plugin may infer mode from user intent when safe; unattended mode requires standard preflight, status-only remains read/report-only, and plugin-improvement review cannot mutate plugin files unless the user explicitly starts plugin-development work.
- (event) Validation passed after plugin run-mode integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved explicit mode-transition rules for the future plugin: narrowing transitions may happen autonomously, including unattended-continuous-run to status-only-report on user return or blocker reporting; broadening transitions require clear authority, including status-only-report back to unattended-continuous-run only with user intent or a recorded continuous-run directive, interactive-intake to unattended-continuous-run only after standard preflight, and plugin-improvement-review to plugin file changes only after explicit plugin-development work starts.
- (event) Validation passed after run-mode transition integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved a project-local authority ledger for continuous runs: a simple HTML/JSON record of active user directives, inferred permissions, mode transitions, continuous-run directives, expiration or supersession notes, and the authority evidence used for broadening transitions so future agents do not rely on stale chat assumptions.
- (event) Validation passed after authority-ledger integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved append-only authority-ledger source entries: agents add new entries for new directives, inferred permissions, expirations, and supersessions; prior source entries are not rewritten in place, while generated HTML or reports may derive current-active, superseded, or expired status from later entries.
- (event) Validation passed after append-only authority-ledger policy integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved slice-local worker reports: each worker produces synchronized JSON and semantic HTML reports before coordinator acceptance or rejection, including assigned paths, changes made, validation run, blockers, proposed shared-control-artifact changes, and authority or resource evidence; proposed shared-control changes remain proposals until coordinator acceptance.
- (event) Validation passed after worker-report artifact integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved a navigation-only project-local control index plus optional rich per-project dashboards: the plugin should prepare bounded dashboard data for external visualization capabilities such as build-web-data-visualization or Data Analytics widgets without hard-coding every visualization model.
- (decision) User approved broad external capability coordination: the plugin coordinator may call, route to, or spawn work through external plugins, skills, APIs, MCP servers, browser tools, external services, local tools, and agents inside authority, credential, supervision, privacy, and evidence boundaries.
- (event) Validation passed after control-index, dashboard-data, and external-capability coordination integration: JSON and HTML artifact checks, rg policy check, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved external capability autonomy and credential-source policy: external capabilities may be used autonomously inside active project authority, development/local boundaries, and existing supervision rules; credentials live in project-local env or JSON files, process environment, or approved ephemeral chat use; durable artifacts record source metadata only and the reusable plugin does not standardize provider-specific credential payloads.
- (event) Validation passed after external capability autonomy and project-local credential-source integration: JSON/HTML structural checks, rg policy checks, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved external capability result packets and dashboard-visible external connections: every external capability invocation is captured in a slice-local worker report or dedicated JSON/HTML result packet with replay/audit metadata, bounded input/output summaries, provenance, and omissions for secrets, credential values, hidden reasoning, PHI, and oversized payloads; dashboard data and generated dashboards visibly list external connections such as APIs, MCP servers, plugins, skills, browser tools, services, local tools, and spawned agents without displaying credentials.
- (event) Validation passed after external capability result-packet and dashboard-visible connection integration: JSON/HTML structural checks, rg policy checks, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved accepted result-packet immutability: after coordinator acceptance, external-capability result packets are immutable source records; corrections, stale results, reruns, or replacement evidence create superseding result packets linked to the prior packet, and dashboards display the current packet while preserving history links to superseded packets.
- (event) Validation passed after accepted result-packet immutability and supersession integration: JSON/HTML structural checks, rg policy checks, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved four-lane status/dashboard model: project-local status and dashboard data must separate internal agent work, external capability activity, validation/evidence, and supervision-required items into distinct lanes with current, blocked, completed, and last-updated summaries while retaining a combined top-level project status.
- (event) Validation passed after four-lane status/dashboard integration: targeted TDD renderer test failed before implementation and passed after, JSON/HTML structural checks passed, rg policy checks passed, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved project-local worker lease model: each active slice records worker ID, branch, assigned paths, lease or heartbeat timestamp, and stale-worker handling; if a worker stalls or disappears, the coordinator marks the lease stale, inspects and preserves slice-owned work, then reassigns or supersedes the affected slice without blocking independent slices.
- (event) Validation passed after worker lease model integration: targeted TDD renderer tests failed before implementation and passed after, JSON/HTML structural checks passed, rg policy checks passed, regenerated run-state HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check.
- (decision) User approved project-local handoff digest HTML artifacts: generate a digest at every stop, compaction-risk point, stale lease, or user-return checkpoint summarizing objective, active authority, queue state, worker leases, blockers, validation, external capabilities, supervision items, and exact resume command or path.
- (event) Validation passed after handoff digest integration: targeted TDD digest renderer test failed before implementation and passed after, JSON/HTML structural checks passed, regenerated run-state and handoff digest HTML, uv run pytest, uv run ruff check ., uv run mypy src, and uv run agentic-wiki docs check all passed.
- (decision) User approved append-only incident/exception logs with PHI-aware monitoring: abnormal coordinator events are captured in project-local JSON/HTML; PHI-handling software uses a HIPAA-focused metadata-only variant with no PHI or secret values, monitoring phases for regular security scans and abnormal-behavior/leakage review, HIPAA best-practice awareness, and an intake question asking whether the application handles PHI.
- (validation) PHI-aware incident/exception log integration validated: uv run pytest (22 passed), uv run ruff check . (passed), uv run mypy src (passed), uv run agentic-wiki docs check (OK), JSON artifacts parsed, and key HTML artifacts contained expected JSON-LD page types.
- (decision) User approved the accelerated inferred decision table as correct and asked to implement it; project-local accepted decision matrix artifacts now carry the accepted rows as source-of-truth requirements, future agents consult them before reopening interview questions, corrections use project-local supersession, and reusable plugin defaults change only through explicit plugin-development workflow.
- (validation) Accepted decision matrix integration validated: decision-matrix JSON parsed, updated HTML artifacts contained JSON-LD, control surfaces linked the matrix, uv run pytest passed with 22 tests, uv run ruff check . passed, uv run mypy src passed, and uv run agentic-wiki docs check returned OK.
- (decision) User authorized following best-practice recommendations for plugin development; coordinator recommended a plugin-development readiness pass before plugin-creator scaffolding, created project-local readiness artifacts for the working plugin name agent-development-coordinator, and preserved the boundary that no plugin files, marketplace entries, cachebusters, or installs occur until explicit plugin-development work starts.
- (validation) Plugin-development readiness integration validated: readiness JSON parsed, updated HTML artifacts contained JSON-LD, control index and dashboard data linked the readiness contract, uv run pytest passed with 22 tests, uv run ruff check . passed, uv run mypy src passed, and uv run agentic-wiki docs check returned OK.
- (event) Local plugin creation started and completed for agent-development-coordinator: plugin-creator scaffolded /Users/tyler-lcsw/plugins/agent-development-coordinator with personal marketplace entry, four local skills and a reusable contract asset were added from project-local readiness artifacts, plugin validation passed, codex plugin add agent-development-coordinator@personal succeeded, and codex plugin list shows the plugin installed and enabled for future threads.
- (validation) Local agent-development-coordinator plugin creation validated: source and installed cache passed plugin-creator validate_plugin.py, marketplace JSON parsed, codex plugin add agent-development-coordinator@personal succeeded, codex plugin list showed installed/enabled, project JSON and HTML JSON-LD checks passed, uv run pytest passed with 22 tests, uv run ruff check . passed, uv run mypy src passed, and uv run agentic-wiki docs check returned OK.
